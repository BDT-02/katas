from task_3 import perimeter
from task_3 import area


radio = float(input("What is the radio of circle?: "))

print "Perimeter is: " + str(perimeter.calculate(radio))
print "Area is: " + str(area.calculate(radio))
